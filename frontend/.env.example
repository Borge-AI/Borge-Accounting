# Local backend (default when not set):
# NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
# For local frontend + Railway backend, copy .env.local.example to .env.local and set the Railway URL.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
