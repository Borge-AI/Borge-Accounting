# Copy this file to .env.local and fill in your backend URL.
# Use this when running the frontend locally (npm run dev) against your deployed Railway backend.

# Railway backend (replace with your actual URL from Railway dashboard):
NEXT_PUBLIC_API_URL=https://YOUR-SERVICE.up.railway.app/api/v1

# Or, if running the backend locally:
# NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
